let firstNumber = +prompt('Введите число от 0 до 100!');

let secondNumber = +prompt('Введите число от 100 до 1000!');

let thirdNumber = +prompt('Введите число от 1000 до 10000!');

let result = (firstNumber + secondNumber + thirdNumber) / 3;

alert(result);